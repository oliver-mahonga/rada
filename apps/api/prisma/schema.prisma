generator client {
  provider     = "prisma-client-js"
  output       = "../src/generated/prisma"
  // This ensures compatibility with NestJS
  moduleFormat = "cjs"
}

datasource db {
  provider = "postgresql"
  url      = env("DATABASE_URL")
}